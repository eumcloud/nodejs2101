doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        h2 메인페이지

        each product in result 
            div.products
                div=product.no 
                div=product.Product_id
                div=product.Category_id
                div=product.Name
                div=product.Price
                div=product.Created_time
                div=product.
                div=
                div=